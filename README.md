# 木头的破壳日网站 🎂

一个专为木头定制的浪漫生日网站，融合了现代Web技术和充满爱意的设计元素。

## 🌟 项目亮点

### 🎨 视觉设计
- **玻璃拟态设计风格**：现代感十足的毛玻璃效果
- **沉浸式Loading体验**：3D相册墙 + 樱花粒子系统  
- **液态数字显示**：动态的恋爱时光机计时器
- **响应式设计**：完美适配手机、平板、电脑

### 💫 核心功能
- **恋爱时光机**：精确计算恋爱天数，展示重要里程碑
- **实时状态监控**：基于Sleepy项目，实时显示当前状态
- **生日庆典模式**：7月16日自动触发烟花和生日祝福
- **互动娱乐区**：留言板、记忆翻牌游戏、默契度测试
- **动态时间线**：记录两人的美好时光

### 🎵 多媒体体验
- **背景音乐系统**：智能音频管理，支持音量控制
- **音效反馈**：丰富的交互音效
- **动画效果**：使用GSAP打造流畅动画

## 🚀 技术架构

### 前端技术栈
```json
{
  "framework": "Nuxt 3 + Vue 3",
  "ui": "Naive UI + 自定义组件",
  "animation": "GSAP + CSS3",
  "charts": "ECharts",
  "audio": "Web Audio API",
  "styling": "SCSS + 响应式设计"
}
```

### 后端服务
- **状态监控**: Python Flask (基于Sleepy项目)
- **数据存储**: JSON文件 + LocalStorage
- **API设计**: RESTful风格

## 📁 项目结构

```
木头的破壳日网站/
├── birthday-website/          # 前端项目
│   ├── components/           # Vue组件
│   │   ├── LoadingScreen.vue    # 3D Loading页面
│   │   ├── LoveTimer.vue        # 恋爱时光机
│   │   ├── StatusCard.vue       # 状态展示卡片
│   │   ├── BirthdayCountdown.vue # 生日倒计时
│   │   ├── BirthdayModal.vue    # 生日庆典模式
│   │   ├── DynamicTimeline.vue  # 动态时间线
│   │   ├── InteractiveZone.vue  # 互动区域
│   │   └── AudioManager.vue     # 音频管理
│   ├── pages/
│   │   └── index.vue           # 主页
│   ├── assets/
│   │   └── css/
│   │       └── main.scss       # 全局样式
│   ├── nuxt.config.ts         # Nuxt配置
│   └── package.json           # 依赖管理
├── sleepy-status/             # 状态监控服务
│   ├── app.py                # Flask应用
│   ├── data.json            # 状态配置
│   └── requirements.txt     # Python依赖
└── README.md                # 项目说明
```

## 🛠️ 开发环境搭建

### 1. 前端项目设置

```bash
# 进入前端项目目录
cd birthday-website

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

访问地址: `http://localhost:3000`

### 2. 状态监控服务部署

```bash
# 进入状态服务目录
cd sleepy-status

# 安装Python依赖
pip install -r requirements.txt

# 启动Flask服务
python app.py
```

服务地址: `http://localhost:5000`

## 📱 状态监控使用说明

### API接口

#### 查询当前状态
```bash
GET http://localhost:5000/query
```

#### 获取状态列表
```bash
GET http://localhost:5000/get/status_list
```

#### 更新状态
```bash
GET http://localhost:5000/set?secret=birthday2024&status=2
```

### 状态配置
```json
{
  "1": {"name": "睡觉中", "desc": "林璐在做美梦zzz...", "color": "#9B59B6"},
  "2": {"name": "工作中", "desc": "林璐正在努力工作", "color": "#3498DB"},
  "3": {"name": "运动中", "desc": "林璐正在运动💪", "color": "#E74C3C"},
  "4": {"name": "看B站", "desc": "林璐正在刷B站", "color": "#FF69B4"},
  "5": {"name": "玩游戏", "desc": "林璐在游戏世界里", "color": "#F39C12"},
  "6": {"name": "听音乐", "desc": "林璐正在享受音乐", "color": "#1ABC9C"}
}
```

## 🎯 移动端状态自动更新

### Android (推荐使用Macrodroid)
1. 安装Macrodroid应用
2. 创建宏规则:
   - 触发器: 应用启动/切换
   - 动作: HTTP请求到状态API
3. 配置对应的APP检测规则

### iOS (使用快捷指令)
1. 打开快捷指令应用
2. 创建个人自动化
3. 添加HTTP请求动作
4. 设置对应的状态更新

## 🎂 生日特效说明

### 自动触发条件
- **木头生日**: 7月16日自动显示庆典模式
- **林璐生日**: 7月27日倒计时提醒

### 特效内容
- 🎆 全屏烟花动画
- 🎵 生日歌自动播放
- 🎁 专属生日礼物盒子
- 💝 个性化生日祝福

## 🎮 互动功能详解

### 1. 留言板
- 物理引擎便利贴效果
- 自动保存到本地存储
- 支持多色彩便利贴

### 2. 记忆翻牌游戏
- 8对情侣主题卡片
- 计时和计分系统
- 最佳成绩记录

### 3. 默契度测试
- 5道精心设计的问题
- 实时评分系统
- 个性化结果反馈

## 🌐 部署到生产环境

### 前端部署
```bash
# 构建生产版本
npm run build

# 生成静态文件
npm run generate

# 部署到服务器
# 将.output/public目录内容上传到Web服务器
```

### 后端部署
```bash
# 使用gunicorn部署Flask应用
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### Nginx配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /path/to/birthday-website/.output/public;
        try_files $uri $uri/ /index.html;
    }
    
    # 状态API代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🔧 自定义配置

### 修改重要日期
在 `components/LoveTimer.vue` 中修改:
```javascript
const loveStartDate = new Date('2022-11-02T00:00:00') // 恋爱开始时间
```

在 `components/BirthdayCountdown.vue` 中修改:
```javascript
const mutouBirthday = new Date('2024-07-16T00:00:00') // 木头生日
const myBirthday = new Date('2024-07-27T00:00:00')     // 我的生日
```

### 添加新状态
在 `sleepy-status/data.json` 中添加:
```json
{
  "9": {
    "name": "新状态",
    "desc": "状态描述",
    "color": "#颜色代码"
  }
}
```

### 更换背景音乐
将音频文件放入 `public/audio/` 目录，并在 `AudioManager.vue` 中更新路径。

## 💡 开发技巧

### 1. 调试状态监控
```bash
# 查看状态页面
curl http://localhost:5000/status_page

# 测试状态更新
curl "http://localhost:5000/set?secret=birthday2024&status=3"
```

### 2. 样式调试
- 使用浏览器开发者工具查看CSS变量
- 修改 `assets/css/main.scss` 中的设计系统变量
- 利用 `breathing` 类添加呼吸灯效果

### 3. 性能优化
- 图片使用WebP格式
- 启用懒加载
- 使用CDN加速资源加载

## 🎉 致谢

这个项目是为了给木头一个特别的生日惊喜而创作的，融合了：
- 现代Web技术的最佳实践
- 充满爱意的设计细节
- 个性化的交互体验

希望木头能够喜欢这个专属的生日礼物！💝

---

**Made with ❤️ for 木头's Birthday**

如有任何问题或建议，欢迎随时交流！